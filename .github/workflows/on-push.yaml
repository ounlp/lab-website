name: on-push
run-name: on push to main

on:
  push:
    branches:
      - main

  # run if user manually requests it
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-citations:
    # skip first run because nothing enabled or setup yet
    if: github.run_number != 1
    uses: ./.github/workflows/update-citations.yaml

  build-site:
    needs: update-citations
    # deploy even if the citation refresh fails or is skipped, so a transient
    # manubot / Google Scholar hiccup never blocks the live site from updating
    if: ${{ always() }}
    uses: ./.github/workflows/build-site.yaml
